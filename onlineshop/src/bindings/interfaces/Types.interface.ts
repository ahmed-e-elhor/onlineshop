export enum roles {
  admin = "admin",
  superAdmin = "superAdmin",
  user = "users",
}

export enum rolesId {
  superAdminRole = 1,
  adminRole = 2,
  userRole = 3,
}

export enum usersTypes {
  adminType = 1,
  userType = 2,
}
